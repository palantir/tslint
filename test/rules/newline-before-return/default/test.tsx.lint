import * as React from 'react';

<div>{ [].map((child: any) => {
    let i = 0;
    return <span />;
    ~nil  [0]
}) }</div>

<div>{ [].map((child: any) => {
    return <span />;
}) }</div>

<div>{ [].map((child: any) =>
    <span />;
) }</div>


[0]: Missing blank line before return
