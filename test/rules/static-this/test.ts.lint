class StaticThis {
    static const staticValue = this.value;
                               ~~~~ [`this` should be replaced with `StaticThis`.]

    static getInstance() {
        return this;
               ~~~~ [`this` should be replaced with `StaticThis`.]
    }

    const value = 5;

    getValue() {
        return this.value;
    }
}
