interface I {
    foo(): void;
    bar<T>(): T;
}

type T = {
    foo?(): void;
}

class C {
    // OK in class
    foo: () => void;
}

export interface Bar {
  someMethod(someArg: Pick<HTMLAllCollection, "item">): string;
}

