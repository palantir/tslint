var x = null; // error
        ~~~~           [Use 'undefined' instead of 'null']
console.log(null, x); // error
            ~~~~               [Use 'undefined' instead of 'null']

let match(): string | null;
interface foo {
    bar: [number, null, string];
}
