const { length } = this;

const { length, toString } = this;

const [foo] = this;

const [foo, bar] = this;

const self = this;
      ~~~~~~~~~~~       [name % ('self')]

[name]: Assigning `this` reference to local variable not allowed: %s.

