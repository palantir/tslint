function foge() {}

class TsLint {
    helloWorld() {
        return 'Hello World';
    };

    arrowHelloWorld = () => {
                      ~~      [expected arrow-call-signature to have a typedef]
        return this.helloWorld();
    };
}

var obj = {
    foge: function() {},
    bar: () => {}
         ~~       [expected arrow-call-signature to have a typedef]
}

setTimeout(() => {}, 1000);
setTimeout(function() {}, 1000);

someFunc(n => n+1);
someFunc(n => {});
