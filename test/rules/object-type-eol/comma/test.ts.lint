interface ITest {
    prop: string;
                ~ [object type members should end with a comma]
    foo?: string
                ~nil [object type members should end with a comma]
    bar: number
               ~nil [object type members should end with a comma]
    baz: boolean,

    func(): void;
                ~ [object type members should end with a comma]

    func2(): string
                   ~nil [object type members should end with a comma]
}

var a: {
    [index: string]: string
                           ~nil [object type members should end with a comma]
}

var b: {
    prop: string
                ~nil [object type members should end with a comma]
}

var c: {
    [index: string]: string;
                           ~ [object type members should end with a comma]
    prop: string
                ~nil [object type members should end with a comma]
}

var d: {
    prop: string;
                ~ [object type members should end with a comma]
    other: string
                 ~nil [object type members should end with a comma]
}

var e: {
    func(): void;
                ~ [object type members should end with a comma]
    func2(): string
                   ~nil [object type members should end with a comma]
}
