interface T { x: number; }

type U = string;
type V = { x: number; } | { y: string; };
export interface W<T> {
    x: T;
}
type Record<T, U> = {
 	[K in T]: U; 
}

export declare interface RequestArgs {
  someKey: string;
  data: {
    num_columns: number,
    column_names: string[],
    column_values: {[key: string]: string},
    key_columns: string[],
  };
  options?: string;
}
