type T = {
    x: number;
    y: string;
};

type T = {
    x: number;
    y: string;
};

type T = {
    x: number;
    y: string;
};

type T = { x: number; y: string };

// Works even when there's extra whitespace
type T = { x: number ; y: number };
type T = { x: number ; y: number  };

