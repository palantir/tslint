var x = (y == y) ? 1 : 2;
         ~~~~~~[0]

var x = (y === y) ? 1 : 2;
         ~~~~~~~[0]

var x = (y != y) ? 1 : 2;
         ~~~~~~ [0]

var x = (y !== y) ? 1 : 2;
         ~~~~~~~ [0]


// function call

if( a.b()  ==  a.b()  ) {
    ~~~~~~~~~~~~~~~~ [0]
}

// Not implemented yet

// Extra space
if((a + b) == (a  + b)) {
}

// Commutativity
if(a + b == b + a) {
}

[0]: Both sides of a binary expression can't be the same
