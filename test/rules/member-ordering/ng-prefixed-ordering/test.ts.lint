class Foo {
    public x: number;
    private y: number;

    constructor() {}
    public m() {}
    private n() {}

    ngOnInit () {}
    ~~~~~~~~~~~~~~ [Declaration of ng instance method not allowed after declaration of instance method. Instead, this should come after constructors.]
}
