class C { }
enum MyEnum {
	A, B, C
}
let anyType: {};
let boolType: boolean;
let boolType2: boolean;
let bwrapType: Boolean;
let numType = 9;
let strType = "string";
let objType = new Object();
let classType = new C();
let enumType = MyEnum.A;
let boolFn = () => { return true; };
let strFn = () => { return strType; };
let numFn = () => { return numType; };
let boolExpr = (strType !== undefined);

/*** Binary Expressions ***/
/*** Invalid Boolean Expressions ***/
classType && boolType;
~~~~~~~~~ [Operand for the && operator is always truthy. Expected a boolean.]
anyType && boolType;
~~~~~~~ [Operand for the && operator is always truthy. Expected a boolean.]
numType && boolType;
~~~~~~~ [Operand for the && operator may be falsy if it is 0. Expected a boolean.]
boolType && strType;
            ~~~~~~~ [Operand for the && operator may be falsy if it is the empty string. Expected a boolean.]
boolType && objType && enumType;
            ~~~~~~~ [Operand for the && operator is always truthy. Expected a boolean.]
                       ~~~~~~~~ [Operand for the && operator is an enum. Expected a boolean.]
bwrapType && boolType;
~~~~~~~~~ [Operand for the && operator is always truthy. Expected a boolean.]

boolType || classType;
            ~~~~~~~~~ [Operand for the || operator is always truthy. Expected a boolean.]
boolType || anyType;
            ~~~~~~~ [Operand for the || operator is always truthy. Expected a boolean.]
boolType || numType;
            ~~~~~~~ [Operand for the || operator may be falsy if it is 0. Expected a boolean.]
strType || boolType;
~~~~~~~ [Operand for the || operator may be falsy if it is the empty string. Expected a boolean.]
bwrapType || boolType;
~~~~~~~~~ [Operand for the || operator is always truthy. Expected a boolean.]
objType || boolType || enumType;
~~~~~~~ [Operand for the || operator is always truthy. Expected a boolean.]
                       ~~~~~~~~ [Operand for the || operator is an enum. Expected a boolean.]

boolExpr && strType;
            ~~~~~~~ [Operand for the && operator may be falsy if it is the empty string. Expected a boolean.]
numType || boolExpr;
~~~~~~~ [Operand for the || operator may be falsy if it is 0. Expected a boolean.]
numType && boolExpr || strType;
~~~~~~~ [Operand for the && operator may be falsy if it is 0. Expected a boolean.]
                       ~~~~~~~ [Operand for the || operator may be falsy if it is the empty string. Expected a boolean.]
bwrapType || boolExpr && bwrapType;
~~~~~~~~~ [Operand for the || operator is always truthy. Expected a boolean.]
                         ~~~~~~~~~ [Operand for the && operator is always truthy. Expected a boolean.]

/*** Valid Boolean Expressions ***/
boolType && boolType2;
boolExpr || boolType;
(numType > 0) && boolFn();
(strType !== "bool") && boolExpr;
(numType > 0) && (strType !== "bool");
(strType !== undefined) || (numType < 0);

/*** ConditionalExpression ***/
/*** Invalid ***/
strType ? strType : numType;
~~~~~~~ [Condition may be falsy if it is the empty string. Expected a boolean.]
numType ? numType : numFn();
~~~~~~~ [Condition may be falsy if it is 0. Expected a boolean.]
objType ? objType : boolExpr;
~~~~~~~ [Condition is always truthy. Expected a boolean.]
classType ? strType : undefined;
~~~~~~~~~ [Condition is always truthy. Expected a boolean.]
bwrapType ? 1 : 0;
~~~~~~~~~ [Condition is always truthy. Expected a boolean.]
enumType ? 0 : 1;
~~~~~~~~ [Condition is an enum. Expected a boolean.]

/*** Valid ***/
boolFn() ? numType : strType;
boolType ? strType : undefined;

/*** PrefixUnary Expressions ***/
/*** Invalid ***/
!!numType;
  ~~~~~~~ [Operand for the ! operator may be falsy if it is 0. Expected a boolean.]
!strType;
 ~~~~~~~ [Operand for the ! operator may be falsy if it is the empty string. Expected a boolean.]
!objType;
 ~~~~~~~ [Operand for the ! operator is always truthy. Expected a boolean.]
!enumType;
 ~~~~~~~~ [Operand for the ! operator is an enum. Expected a boolean.]
!!classType;
  ~~~~~~~~~ [Operand for the ! operator is always truthy. Expected a boolean.]
!bwrapType;
 ~~~~~~~~~ [Operand for the ! operator is always truthy. Expected a boolean.]
!!undefined;
 ~~~~~~~~~~ [Operand for the ! operator is always truthy. Expected a boolean.]
  ~~~~~~~~~ [Operand for the ! operator is always falsy. Expected a boolean.]

/*** Valid ***/
!!boolFn();
!boolExpr;
!!boolType;

/*** If Statement ***/
/*** Invalid ***/
if (numType) { /* statements */ }
    ~~~~~~~ [If statement condition may be falsy if it is 0. Expected a boolean.]
if (objType) { /* statements */ }
    ~~~~~~~ [If statement condition is always truthy. Expected a boolean.]
if (strType) { /* statements */ }
    ~~~~~~~ [If statement condition may be falsy if it is the empty string. Expected a boolean.]
if (bwrapType) { /* statements */ }
    ~~~~~~~~~ [If statement condition is always truthy. Expected a boolean.]
if (strFn()) { /* statements */ }
    ~~~~~~~ [If statement condition may be falsy if it is the empty string. Expected a boolean.]
if (MyEnum.A) { /* statements */ }
    ~~~~~~~~ [If statement condition is an enum. Expected a boolean.]
if (classType) { /* statements */ }
    ~~~~~~~~~ [If statement condition is always truthy. Expected a boolean.]

/*** Valid ***/
if (boolFn()) { /* statements */ }
if (boolExpr) { /* statements */ }
if (boolType) { /* statements */ }

/*** While Statement ***/
/*** Invalid ***/
while (numType) { /* statements */ }
       ~~~~~~~ [While statement condition may be falsy if it is 0. Expected a boolean.]
while (objType) { /* statements */ }
       ~~~~~~~ [While statement condition is always truthy. Expected a boolean.]
while (strType) { /* statements */ }
       ~~~~~~~ [While statement condition may be falsy if it is the empty string. Expected a boolean.]
while (strFn()) { /* statements */ }
       ~~~~~~~ [While statement condition may be falsy if it is the empty string. Expected a boolean.]
while (bwrapType) { /* statements */ }
       ~~~~~~~~~ [While statement condition is always truthy. Expected a boolean.]
while (MyEnum.A) { /* statements */ }
       ~~~~~~~~ [While statement condition is an enum. Expected a boolean.]
while (classType) { /* statements */ }
       ~~~~~~~~~ [While statement condition is always truthy. Expected a boolean.]

/*** Valid ***/
while (boolFn()) { /* statements */ }
while (boolExpr) { /* statements */ }
while (boolType) { /* statements */ }

/*** Do Statement ***/
/*** Invalid ***/
do { /* statements */ } while (numType);
                               ~~~~~~~ [Do-While statement condition may be falsy if it is 0. Expected a boolean.]
do { /* statements */ } while (objType);
                               ~~~~~~~ [Do-While statement condition is always truthy. Expected a boolean.]
do { /* statements */ } while (strType);
                               ~~~~~~~ [Do-While statement condition may be falsy if it is the empty string. Expected a boolean.]
do { /* statements */ } while (bwrapType);
                               ~~~~~~~~~ [Do-While statement condition is always truthy. Expected a boolean.]
do { /* statements */ } while (strFn());
                               ~~~~~~~ [Do-While statement condition may be falsy if it is the empty string. Expected a boolean.]
do { /* statements */ } while (MyEnum.A);
                               ~~~~~~~~ [Do-While statement condition is an enum. Expected a boolean.]
do { /* statements */ } while (classType);
                               ~~~~~~~~~ [Do-While statement condition is always truthy. Expected a boolean.]

/*** Valid ***/
do { /* statements */ } while (boolFn());
do { /* statements */ } while (boolExpr);
do { /* statements */ } while (boolType);

/*** For Statement ***/
/*** Invalid ***/
for (let j = 0; j; j++) { /* statements */ }
                ~                            [For statement condition may be falsy if it is 0. Expected a boolean.]
/*** Valid ***/
for (let j = 0; j > numType; j++) { /* statements */ }
