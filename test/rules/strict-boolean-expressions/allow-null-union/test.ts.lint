declare function get<T>(): T;

if (get<RegExp | null>()) {}

if (get<boolean | null>()) {}
    ~~~~~~~~~~~~~~~~~~~~~ [If statement condition mixes multiple kinds of falsy values. Expected a boolean or null-union.]

if (get<null>()) {}
    ~~~~~~~~~~~ [If statement condition is always falsy. Expected a boolean or null-union.]

if (get<RegExp | undefined>()) {}
    ~~~~~~~~~~~~~~~~~~~~~~~~~ [If statement condition may be falsy if it is undefined. Expected a boolean or null-union.]
