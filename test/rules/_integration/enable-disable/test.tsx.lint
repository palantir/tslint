const span = (
    <span>
        without this text test fails
        /* tslint:disable */
        {
            x + 'test'
                ~~~~~~ [' should be "]
        }
        text
    </span>
);

const span = (
    <span>
        without this text test fails
        // tslint:disable-next-line
        {x + 'test'}
             ~~~~~~ [' should be "]
        text
    </span>
);