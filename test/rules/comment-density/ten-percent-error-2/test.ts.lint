namespace App {
    class GiveMeANumber {

        // comment
        private num:any = 10;

        constructor(){
            this.giveNumber();
        }

        public giveNumber = () =>{
            alert(this.num);
        };
    }
}
~ [This file has 7% of comments, which is lower than the minimum of 10% allowed]