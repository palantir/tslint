class Clazz { // This comment is correct
    /* block comment
     * adada
     */
    public funcxion() { // this comment has a lower   letter starting it
        //this comment is on its own line, and starts with a lower   _and_ no space
        console.log("test"); //This comment has no space
    }
    /// <reference or something>
}

//#region test
//#endregion

`${location.protocol}//${location.hostname}`
