/// <reference path='fourslash.ts' />

////export class encoder {
////    static getEncoding(buffer: buffer/**/Pointer
////}

goTo.marker();
// Bug 17112: Intellisense for the value of a type shows all identifiers, not just types
// not able to repro on LS level - need to investigate
//verify.not.memberListContains('btoa');
//debug.printCompletionListMembers();