var _modes;
(function (_modes) {
    var Mode = (function () {
        function Mode() {
        }
        return Mode;
    })();
})(_modes || (_modes = {}));

var editor;
(function (editor) {
    var modes = _modes;

    var i;

    var Bug = (function () {
        function Bug(p1, p2) {
            var x;
        }
        return Bug;
    })();
})(editor || (editor = {}));
