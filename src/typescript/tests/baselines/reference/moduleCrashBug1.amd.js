var _modes;
(function (_modes) {
    var Mode = (function () {
        function Mode() {
        }
        return Mode;
    })();
})(_modes || (_modes = {}));

var editor;
(function (editor) {
    var modes = _modes;
})(editor || (editor = {}));

var m;
